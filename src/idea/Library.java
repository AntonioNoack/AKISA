package idea;

public class Library {
	/*public static final File mainfolder = new File("C:/Users/Antonio/Documents/AKISA/library");
	public static HashMap<Integer, String[]> data = new HashMap<>();
	public static String get(int i){
		int a=i/1024, b=i-a*1024;
		if(!data.containsKey(a)){
			String[] names = new String[1024];
			File file = new File(mainfolder, a+".dat");
			if(file.exists()){
				try {
					int index=0;
					BufferedReader read = new BufferedReader(new FileReader(file));
					for(String line=read.readLine();line!=null;line=read.readLine()){
						if(line.length()>0){
							names[index++]=line;
						}
					}
					read.close();
				} catch (IOException e) {
					e.printStackTrace();
				}
			}
			data.put(a, names);
		}
		return data.get(a)[b];
	}
	
	public static void add(String s){
		if(s.length()>0){
			
		}
	}*/
}
